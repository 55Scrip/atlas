# Atlas Infrastructure

This directory is reserved for deployment, observability, and environment
configuration.

No production infrastructure is introduced in Sprint 36. CI lives in
`.github/workflows/ci.yml` and local quality hooks are configured through
`.pre-commit-config.yaml`.

