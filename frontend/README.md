# Atlas Frontend

This directory is reserved for future user-facing applications.

Sprint 36 establishes strict TypeScript configuration now so a future interface
can be added without changing backend architecture. No frontend runtime is
implemented in this sprint.

